{% extends '_template/base.html.twig' %}

{% block body %} 
<div class="container my-5">
    <div class="row">
        <!-- Formulaire de contact -->
        <div class="col-md-6">
            <h4>Nous contacter</h4>
            <form method="POST" action="">
                <div class="mb-3">
                    <label for="subject" class="form-label">Sujet :</label>
                    <input type="text" id="subject" name="subject" class="form-control">
                </div>
                <div class="mb-3">
                    <textarea class="form-control" id="content" name="content" rows="5"></textarea>
                </div>
                {% if user %}
                    <button type="submit" class="btn btn-secondary">Envoyer</button>
                {% else %}
                    <p class="text-muted">Connectez-vous pour envoyer un message.</p>
                {% endif %}
            </form>
        </div>
        
        <!-- Section A propos avec FAQ -->
        <div class="col-md-6 border-start ps-4">
            <h4>A propos</h4>
            <div class="accordion" id="faqAccordion">
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#question1">
                            Quels sont les moyens de nous contacter ?
                        </button>
                    </h2>
                    <div id="question1" class="accordion-collapse collapse show" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            Vous pouvez nous contacter via notre <strong>Instagram</strong>, sur <strong>Discord</strong>, ou directement via la rubrique <strong>"Contact"</strong> située juste à gauche.
                        </div>
                    </div>
                </div>
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#question2">
                            Comment s'inscrire ?
                        </button>
                    </h2>
                    <div id="question2" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            Pour vous inscrire, il vous suffit de vous rendre sur la page d'inscription et de remplir le formulaire.
                        </div>
                    </div>
                </div>
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#question3">
                            Quels sont les avantages d'être membre ?
                        </button>
                    </h2>
                    <div id="question3" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
                        <div class="accordion-body">
                            En tant que membre, vous aurez accès à des réductions sur nos produits et services.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
